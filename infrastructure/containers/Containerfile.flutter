FROM ghcr.io/cirruslabs/flutter:3.24.3

WORKDIR /workspace/apps/mobile

# Copy mobile app definition
COPY apps/mobile ./

# Run non-interactive pub get and analyze
RUN flutter doctor -v
RUN flutter pub get
RUN flutter analyze

# Default command runs all widget and unit tests
CMD ["flutter", "test"]
