apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 5s

  - name: OpenTelemetry
    type: tempo
    access: proxy
    url: http://otel-collector:4318
    isDefault: false
