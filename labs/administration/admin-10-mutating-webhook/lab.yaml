id: admin-10-mutating-webhook
title: Mutating Admission Controller Webhook
difficulty: intermediate
duration_minutes: 20
track: administration
objectives:
- Master Mutating Admission Controller Webhook in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: Deploy mutating webhook to inject default sidecars
tasks:
- id: task-verify-admin-10-mutating-webhook
  title: Verify Mutating Admission Controller Webhook
  description: Verify metadata.name on mutatingwebhookconfigurations 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: mutatingwebhookconfigurations
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `mutatingwebhookconfigurations` resource specifications.
  penalty_points: 10
solution: kubectl apply -f admin-10-mutating-webhook.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
