id: helm-04-lifecycle-hooks
title: Database Migration Lifecycle Hooks
difficulty: intermediate
duration_minutes: 20
track: helm-kustomize
objectives:
- Master Database Migration Lifecycle Hooks in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: Execute pre-install database schema migration job
tasks:
- id: task-verify-helm-04-lifecycle-hooks
  title: Verify Database Migration Lifecycle Hooks
  description: Verify metadata.name on jobs 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: jobs
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `jobs` resource specifications.
  penalty_points: 10
solution: kubectl apply -f helm-04-lifecycle-hooks.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
