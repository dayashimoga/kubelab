id: kust-02-patches-strategic
title: Strategic Merge & JSON6902 Patching
difficulty: intermediate
duration_minutes: 20
track: helm-kustomize
objectives:
- Master Strategic Merge & JSON6902 Patching in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: Patch replica counts and image tags per environment
tasks:
- id: task-verify-kust-02-patches-strategic
  title: Verify Strategic Merge & JSON6902 Patching
  description: Verify metadata.name on deployments 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: deployments
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `deployments` resource specifications.
  penalty_points: 10
solution: kubectl apply -f kust-02-patches-strategic.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
