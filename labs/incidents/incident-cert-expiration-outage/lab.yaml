id: incident-cert-expiration-outage
title: 'Production Incident: Expired Control Plane TLS Certificates'
difficulty: advanced
duration_minutes: 15
track: incidents
scenario: 'SEV-1 War Room: Renew expired kube-apiserver certificates and restore API
  connectivity.'
objectives:
- 'Master Production Incident: Expired Control Plane TLS Certificates architecture
  and declarative configurations'
- Diagnose and remediate common production gotchas in incident-cert-expiration-outage
- Verify live Kubernetes cluster state deterministically
prerequisites: []
environment:
  type: kubernetes
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
initial_state:
  manifests:
  - apiVersion: v1
    kind: Namespace
    metadata:
      name: lab-incident-cert-expiration-outage
      labels:
        kubelab.io/track: incidents
        kubelab.io/lab: incident-cert-expiration-outage
tasks:
- id: task-01-incident-cert-expiration-outage
  title: 'Deploy & Configure Production Incident: Expired Control Plane TLS Certificates'
  description: Apply the declarative manifest for incident-cert-expiration-outage
    and ensure resource convergence.
  points: 50
  validation:
    type: k8s_resource
    resource: deployments
    name: incident-cert-expiration-outage
    namespace: lab-incident-cert-expiration-outage
    assertions:
    - field: status.readyReplicas
      operator: greater_than_or_equal
      expected: 1
- id: task-02-incident-cert-expiration-outage
  title: 'Verify Production Hardening & Labels for Production Incident: Expired Control
    Plane TLS Certificates'
  description: Verify metadata labels and security compliance in namespace lab-incident-cert-expiration-outage.
  points: 50
  validation:
    type: k8s_resource
    resource: deployments
    name: incident-cert-expiration-outage
    namespace: lab-incident-cert-expiration-outage
    assertions:
    - field: metadata.labels.app
      operator: equals
      expected: incident-cert-expiration-outage
hints:
- text: Inspect the live objects with `kubectl get all -n lab-incident-cert-expiration-outage
    -o wide`
  penalty_points: 10
- text: Review detailed error events using `kubectl describe pods -l app=incident-cert-expiration-outage
    -n lab-incident-cert-expiration-outage`
  penalty_points: 20
solution: kubectl apply -f https://raw.githubusercontent.com/dayashimoga/kubelab/main/labs/incidents/incident-cert-expiration-outage/solution.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  runAsNonRoot: true
  allowPrivilegeEscalation: false
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
