id: mesh-04-header-routing-canary
title: Header-Based (A/B Testing) Route Matching
difficulty: intermediate
duration_minutes: 20
track: service-mesh
objectives:
- Master Header-Based (A/B Testing) Route Matching in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: 'Route requests with x-canary: true header to staging'
tasks:
- id: task-verify-mesh-04-header-routing-canary
  title: Verify Header-Based (A/B Testing) Route Matching
  description: Verify metadata.name on virtualservices 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: virtualservices
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `virtualservices` resource specifications.
  penalty_points: 10
solution: kubectl apply -f mesh-04-header-routing-canary.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
