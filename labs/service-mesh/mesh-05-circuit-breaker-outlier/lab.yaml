id: mesh-05-circuit-breaker-outlier
title: Circuit Breaking & Outlier Detection
difficulty: intermediate
duration_minutes: 20
track: service-mesh
objectives:
- Master Circuit Breaking & Outlier Detection in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: Eject failing pods after 3 consecutive 5xx server errors
tasks:
- id: task-verify-mesh-05-circuit-breaker-outlier
  title: Verify Circuit Breaking & Outlier Detection
  description: Verify metadata.name on destinationrules 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: destinationrules
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `destinationrules` resource specifications.
  penalty_points: 10
solution: kubectl apply -f mesh-05-circuit-breaker-outlier.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
