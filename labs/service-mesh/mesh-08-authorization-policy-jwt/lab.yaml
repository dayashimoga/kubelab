id: mesh-08-authorization-policy-jwt
title: Microsegmentation with JWT AuthorizationPolicies
difficulty: intermediate
duration_minutes: 20
track: service-mesh
objectives:
- Master Microsegmentation with JWT AuthorizationPolicies in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: Allow access only to authenticated JWT Bearer tokens
tasks:
- id: task-verify-mesh-08-authorization-policy-jwt
  title: Verify Microsegmentation with JWT AuthorizationPolicies
  description: Verify metadata.name on authorizationpolicies 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: authorizationpolicies
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `authorizationpolicies` resource specifications.
  penalty_points: 10
solution: kubectl apply -f mesh-08-authorization-policy-jwt.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
