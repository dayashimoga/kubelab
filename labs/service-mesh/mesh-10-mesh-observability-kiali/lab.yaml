id: mesh-10-mesh-observability-kiali
title: Mesh Topology Visualization with Kiali & Jaeger
difficulty: intermediate
duration_minutes: 20
track: service-mesh
objectives:
- Master Mesh Topology Visualization with Kiali & Jaeger in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: Inspect service dependency graph and mTLS lock status
tasks:
- id: task-verify-mesh-10-mesh-observability-kiali
  title: Verify Mesh Topology Visualization with Kiali & Jaeger
  description: Verify metadata.name on deployments 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: deployments
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `deployments` resource specifications.
  penalty_points: 10
solution: kubectl apply -f mesh-10-mesh-observability-kiali.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
