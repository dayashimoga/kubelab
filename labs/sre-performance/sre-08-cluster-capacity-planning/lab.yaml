id: sre-08-cluster-capacity-planning
title: Cluster Sizing, Overcommit & Cluster Autoscaler
difficulty: intermediate
duration_minutes: 20
track: sre-performance
objectives:
- Master Cluster Sizing, Overcommit & Cluster Autoscaler in Kubernetes
- Execute and verify deterministic live cluster state
prerequisites: []
environment:
  cluster: disposable
  namespace_isolation: true
  resources:
    cpu_limit: 500m
    memory_limit: 512Mi
  type: kubernetes
initial_state:
  manifests: []
scenario: Configure node autoscaling thresholds and resource quotas
tasks:
- id: task-verify-sre-08-cluster-capacity-planning
  title: Verify Cluster Sizing, Overcommit & Cluster Autoscaler
  description: Verify metadata.name on resourcequotas 'target-resource'
  points: 100
  validation:
    type: k8s_resource
    resource: resourcequotas
    name: target-resource
    namespace: null
    assertions:
    - field: metadata.name
      operator: equals
      expected: target-resource
      actual: null
      passed: null
      error_message: null
hints:
- text: Inspect Kubernetes documentation for `resourcequotas` resource specifications.
  penalty_points: 10
solution: kubectl apply -f sre-08-cluster-capacity-planning.yaml
cleanup:
  auto: true
limits:
  max_attempts: 5
  timeout_minutes: 25
security:
  allowPrivilegeEscalation: false
  runAsNonRoot: true
  seccompProfile: RuntimeDefault
resources:
  cpu: 250m
  memory: 256Mi
tested_versions:
- v1.28.0
- v1.29.0
- v1.30.0
